---
description: Follow subagent driven development workflow
---

<rules>
  <rule>You MUST call the skills_subagent_driven_development tool before doing anything else</rule>
  <rule>You MUST explicitly instruct programmer subagents to call the skills_test_driven_development tool before starting their task</rule>
  <rule You MUST call the skills_requesting_code_review tool before requesting a code review</rule>
</rules>

Implement the attached plan.
$ARGUMENTS
